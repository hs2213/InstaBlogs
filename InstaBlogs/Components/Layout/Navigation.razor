<div class="nav">
    <h1>I</h1>
    <h1>B</h1>
    
    <div class="icons">
        <i @onclick="@(() => NavigationManager.NavigateTo("", true))" class="bi bi-house"></i>
        <i class="bi bi-search"></i>
        <RoleView RoleToDisplay="[Role.Admin]">
            <ChildContent>
                <i class="bi bi-check-all" @onclick="@(() => NavigationManager.NavigateTo("approve-reject", true))"></i>
            </ChildContent>
            <WrongRoleContent></WrongRoleContent>
        </RoleView>
        <i @onclick="@(() => NavigationManager.NavigateTo("create"))" class="bi bi-plus-circle"></i>
        <i class="bi bi-person" @onclick="@(() => NavigationManager.NavigateTo("profile"))"></i>
    </div>
    
    @if (NavigationManager.Uri.Contains("register") == false)
    { 
        <Login/>
    }
</div>